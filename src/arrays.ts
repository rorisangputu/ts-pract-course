let prices: number[] = [100, 75, 43, 80]; //type array of numbers

let fruit: string[] = ['apples', 'oranges']
fruit.push('pears');
console.log(fruit);

let array: (string | boolean)[] = ['apples', true, 'bread', false];

//Challenge
 
let temperatures: number[] = [20, 25, 30];
// temperatures.push('hot');

let colors: string[] = ['red', 'green', 'blue'];
// colors.push(true);

let mixedArray: (number | string)[] = [1, 'two', 3];
